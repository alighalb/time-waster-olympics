{
  "name": "time-waster-olympics",
  "version": "2.0.0",
  "private": true,
  "description": "Time-Waster Olympics \u2014 Vercel serverless backend (Postgres + JWT)",
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "start": "vercel dev",
    "test": "node --experimental-test-module-mocks --test \"test/*.test.js\""
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.13.0"
  }
}
